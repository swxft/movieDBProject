"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function toBeArrayOfUniqueItems(received) {
    var pass = new Set(received).size === received.length;
    return {
        pass: pass,
        message: function () {
            return pass
                ? 'Expected the array to include duplicate items, but all items were unique'
                : 'Expected the array to contain only unique items, but duplicates were found';
        },
    };
}
expect.extend({
    toBeArrayOfUniqueItems: toBeArrayOfUniqueItems,
});
