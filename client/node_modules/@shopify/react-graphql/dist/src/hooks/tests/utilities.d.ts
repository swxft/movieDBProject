import { GraphQL } from '@shopify/graphql-testing';
interface Options {
    graphQL?: GraphQL;
    skipInitialGraphQL?: boolean;
}
interface Context {
    graphQL: GraphQL;
}
export declare const mountWithGraphQL: import("@shopify/react-testing").CustomMount<Options, Context, true>;
export declare function createResolvablePromise<T>(value: T): {
    resolve: () => Promise<T>;
    reject: () => void;
    promise: Promise<T>;
};
export {};
//# sourceMappingURL=utilities.d.ts.map