import React from 'react';
export class TestWrapper extends React.Component {
  constructor(...args) {
    super(...args);
    this.state = {};
  }

  // eslint-disable-next-line @shopify/react-prefer-private-members
  setProps(props) {
    this.setState({
      props
    });
  }

  render() {
    const {
      props
    } = this.state;
    const {
      children,
      render
    } = this.props;
    return render(props ? /*#__PURE__*/React.cloneElement(children, props) : children);
  }

}